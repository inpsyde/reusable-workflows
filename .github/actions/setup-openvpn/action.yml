name: Set up OpenVPN
description: Install OpenVPN and establish a VPN connection.

inputs:
  ovpn-config:
    description: OpenVPN configuration file content.
    required: true
  ovpn-username:
    description: OpenVPN username for authentication.
    required: true
  ovpn-password:
    description: OpenVPN password for authentication.
    required: true
  ovpn-gateway-ip:
    description: Expected IP address of the VPN gateway.
    required: true

runs:
  using: composite
  steps:
    - name: Install OpenVPN
      run: |
        sudo apt update
        sudo apt install -y openvpn openvpn-systemd-resolved
      shell: bash

    - name: Write OpenVPN config
      env:
        OVPN_CONFIG: ${{ inputs.ovpn-config }}
      run: |
        echo "$OVPN_CONFIG" > /tmp/client.ovpn
        chmod 600 /tmp/client.ovpn
      shell: bash

    - name: Connect
      uses: "kota65535/github-openvpn-connect-action@cd2ed8a90cc7b060dc4e001143e811b5f7ea0af5" # v3.1.0
      with:
        config_file: /tmp/client.ovpn
        username: ${{ inputs.ovpn-username }}
        password: ${{ inputs.ovpn-password }}

    - name: Verify VPN egress IP
      env:
        GATEWAY_IP: ${{ inputs.ovpn-gateway-ip }}
      run: |
        [[ -f openvpn.log ]] || { echo "openvpn.log not found — cannot verify gateway"; exit 1; }
        grep -q "Initialization Sequence Completed" openvpn.log || { echo "OpenVPN did not report a completed connection"; cat openvpn.log; exit 1; }
        LAST_PEER=$(grep "Peer Connection Initiated with \[AF_INET\]" openvpn.log | tail -1 || true)
        echo "$LAST_PEER" | grep -q "\[AF_INET\]${GATEWAY_IP}:" || { echo "OpenVPN's last connection attempt was not to the expected gateway ($GATEWAY_IP)"; cat openvpn.log; exit 1; }
        echo "Connected to expected gateway: $GATEWAY_IP"
      shell: bash

    - name: Delete OpenVPN config
      if: always()
      run: rm -f /tmp/client.ovpn
      shell: bash
